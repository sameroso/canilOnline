import styled from 'styled-components'

export default {
  Container: styled.div`
    max-width: 800px;
    display: flex;
    margin: auto;
    overflow: hidden;
    flex-wrap: wrap;
    justify-content: space-around;
  `,
}
